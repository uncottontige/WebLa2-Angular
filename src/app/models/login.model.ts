export interface Login {
    access_token: string,
    refresh_token: string,
    idToken: string,
    refreshToken: string,
    expires_in: string,
    expiresIn: string,
    statusCode: string,
    error: string,
    message: string
}